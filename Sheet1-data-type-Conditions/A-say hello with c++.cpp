#include<bits/stdc++.h>
using namespace std;

int main() {
    string name;
    cin >> name;  // Read the name

    // Print the greeting message
    cout << "Hello, " << name << endl;

    return 0;
}
